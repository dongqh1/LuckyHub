🚀 LuckyHub（2026-07-11～2026-07-12）
Progress: 已完成 Java 17/Maven、Docker Desktop、MySQL 8.4（localhost:3307）、
Redis 7.4（localhost:6379）、Flyway V1 的 11 张表、MyBatis-Plus 3.5.17、
统一响应/异常/参数校验/Request ID/OpenAPI、RBAC 三个实体与 Mapper、BCrypt
工作因子 12 的 PasswordService；此前自动化测试达到 28 个全部通过。今天用户手动新增了
ValidPassword、PasswordValidator、CreateUserCommand、UserView、UserService
、UserController、UserServiceImpl，当前 Controller 和 Service 实现文件存在，
但尚未创建 UserControllerTests 与 UserServiceTests。
Plans: 明天先停止并重启 IDEA 中的 LuckyhubApplication，
确认 POST http://localhost:8080/api/admin/users 且 /v3/api-docs 中存在该路径；
随后创建 UserControllerTests 和 UserServiceTests，分别验证 HTTP 201/400、BCrypt
落库、重复用户名 409 和事务回滚，最后运行 Java 17 全量测试并用 Apifox 复测成功、
参数错误、重复用户名三种请求。
Problems: 当前 Apifox 请求返回 code=30002“请求的资源不存在”，
说明请求到达应用但运行中的 Spring Context 没有匹配到路由，优先怀疑旧进程未重启、
请求方法/URL 不一致或 Controller 未被当前进程加载；UserServiceImpl 位于包
`com.dongqh.luckyhub.rbac.service.Impl`（大写 Impl），应在验证时规范为小写
`impl`；当前后台创建用户接口尚无认证保护，只能本机使用，JWT + Redis Session
完成后必须保护该接口。
已经解决了这个code=30002问题了。
